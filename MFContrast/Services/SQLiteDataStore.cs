using System;
namespace MFContrast.Services
{
    public class SQLiteDataStore
    {
        public SQLiteDataStore()
        {
            // in class example
            // var databaseFile = GetCurrentDataBaseFile();

            // studentConnection = new SQLiteAsyncConnection(GetFull)
        }
    }
}
